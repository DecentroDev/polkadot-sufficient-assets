import { beforeEach } from 'node:test';
import { describe, it } from 'vitest';

describe('config context', () => {
  beforeEach(() => {});

  it('should render Config Provider successfully', () => {});
});
